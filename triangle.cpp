#include <iostream>
#include <cmath>
using namespace std;
float Tarea(float l,float b,float h)
{

}
float parameter(float l,float b, float h)
{
    float s;
      s = (l + b + h) 
    return (s);
}

int main()
{
    float a, b, c;
    cout << "Enter the sides of the triangle" << endl;
    cin >> a >> b >> c;
    parameter (a,b,c);
    return 0;
}